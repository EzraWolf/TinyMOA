[project]
name = "tinymoa-cpu-tests"
version = "0.1.0"
description = "Test suite for TinyMOA CPU"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cocotb>=2.0.1",
    "cocotb-test>=0.2.6",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
    "toml>=0.10.2",
]
