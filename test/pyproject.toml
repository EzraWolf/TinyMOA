[project]
name = "TinyMOA-TestBench"
version = "0.1.0"
description = "Test package using CoboTB for TinyMOA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cocotb>=2.0.1",
    "numpy>=2.4.2",
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.1",
]
