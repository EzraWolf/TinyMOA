[project]
name = "tinymoa-tests"
version = "0.1.0"
description = "RTL test suite for TinyMOA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cocotb>=2.0.1",
    "cocotb-test>=0.2.6",
    "pytest>=9.0.3",
    "ruff>=0.15.15",
    "toml>=0.10.2",
]
