[project]
name = "tinymoa-rtl-tests"
version = "0.1.0"
description = "TinyMOA RTL test suite"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "cocotb>=2.0.1",
    "cocotb-test>=0.2.6",
    "pytest>=9.0.3",
    "pytest-sugar>=1.1.1",
    "pytest-xdist>=3.8.0",
    "ruff>=0.15.15",
    "toml>=0.10.2",
]
