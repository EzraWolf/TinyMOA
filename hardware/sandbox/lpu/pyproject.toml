[project]
name = "tinymoa-lpu-sandbox"
version = "0.1.0"
description = "TinyMOA LPU cycle-accurate model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
