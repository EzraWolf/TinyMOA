[project]
name = "tinymoa-cpu-sandbox"
version = "0.1.0"
description = "TinyMOA CPU cycle-accurate model"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
