[project]
name = "tinymoa-cpu-sandbox"
version = "0.1.0"
description = "Sandbox for the TinyMOA CPU"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.2",
    "ruff>=0.15.2",
]
