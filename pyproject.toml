[project]
name = "TinyMOA"
version = "0.1.0"
description = "LLM inference SoC"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "cocotb>=2.0.1",
    "cocotb-test>=0.2.6",
    "pytest>=9.0.3",
    "ruff>=0.15.2",
    "toml>=0.10.2",
]

[tool.uv.workspace]
members = [
    "hardware/sandbox/cpu",
    "hardware/sandbox/lpu",
    "hardware/tests",
]
