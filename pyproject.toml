[project]
name = "TinyMOA"
version = "0.1.0"
description = "Nibble-serial RV32EC CPU with compute-in-memory (CIM) accelerator."
readme = "README.md"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "cocotb>=2.0.1",
    "cocotb-test>=0.2.6",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
]
